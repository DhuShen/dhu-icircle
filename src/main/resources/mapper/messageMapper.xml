<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhu.dao.MessageDao">
    <insert id="insertMessage">
        insert into message(messageContent, messageUserIdSet, messageUserIdGet, messageSetTime)
        values (#{messageContent}, #{messageUserIdSet}, #{messageUserIdGet}, #{messageSetTime})
    </insert>
    
    <select id="selectByGetId" parameterType="String" resultType="Message">
        select *
        from message
        where messageUserIdGet = #{getId}
    </select>
</mapper>