<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhu.dao.PostDao">

    <insert id="insertPost">
        insert into post(postName, postContent, postKey, postGood, postTime, post_UserId, post_CircleId)
        values (#{postName}, #{postContent}, #{postKey}, #{postGood}, #{postTime}, #{post_UserId}, #{post_CircleId})
    </insert>

    <select id="selectByName" parameterType="String" resultType="Post">
        <bind name="pattern" value="'%' + postName + '%'"/>
        select * from post where postName like #{pattern}
    </select>

    <select id="selectTen" resultType="Post">
        select *
        from post
        ORDER BY postGood DESC
        limit 0,10
    </select>

    <select id="selectByCircle" resultType="Post">
        select *
        from post
        where post_CircleId = #{circleId}
    </select>

    <select id="selectByUser" resultType="Post">
        select *
        from post
        where post_UserId = #{userId}
    </select>

    <update id="likePost">
        update post
        set postGood=postGood + 1
        where postId = #{postId}
    </update>

    <select id="selectById" resultType="Post">
        select *
        from post
        where postId = #{postId}
    </select>
    <select id="getPostCount" resultType="java.lang.Integer">
        select COUNT(*)
        from post
        where post_CircleId = #{circleId}
    </select>
    <select id="selectKeyByCircle" resultType="com.dhu.domain.Post">
        select *
        from post
        where post_CircleId = #{circleId}
          and postKey = 1
    </select>

    <update id="setPostKey">
        update post
        set postKey=1
        where postId = #{postId}
    </update>

    <delete id="cancelPostKey">
        update post
        set postKey=0
        where postId = #{postId}
    </delete>

    <delete id="cancelPost">
        delete
        from post
        where postId = #{postId}
    </delete>
</mapper>