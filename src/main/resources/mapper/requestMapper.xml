<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhu.dao.RequestDao">
    <insert id="insertRequest" parameterType="Request">
        insert into request(requestCircleId, requestCircleName, requestCircleContent, requestType, requestUserId,
                            requestTime, request_UserId)
        values (#{requestCircleId}, #{requestCircleName}, #{requestCircleContent}, #{requestType}, #{requestUserId},
                #{requestTime}, #{request_UserId})
    </insert>
</mapper>