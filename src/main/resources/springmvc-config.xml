<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <mvc:annotation-driven/>

    <!--资源放行-->
    <mvc:resources mapping="/js/**" location="/js/"/>
    <mvc:resources mapping="/lib/**" location="/lib/"/>
    <mvc:resources mapping="/css/**" location="/css/"/>
    <mvc:resources mapping="/pages/**" location="/pages/"/>

    <!--包扫描-->
    <context:component-scan base-package="com.dhu.controller"/>
    <context:component-scan base-package="com.dhu.advice"/>

    <!--视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/pages/"/>
    </bean>

    <!--拦截器-->
    <mvc:interceptors>
        <mvc:interceptor><!--user-->
            <mvc:mapping path="/recommend"/>
            <mvc:mapping path="/circles"/>
            <mvc:mapping path="/mail"/>
            <mvc:mapping path="/info"/>
            <mvc:mapping path="/pinfo"/>
            <mvc:mapping path="/singleCircle"/>
            <mvc:mapping path="/post"/>
            <mvc:mapping path="/search"/>
            <bean class="com.dhu.interceptor.UserInterceptor"/>
        </mvc:interceptor>

        <mvc:interceptor><!--admin-->
            <mvc:mapping path="/adminHome"/>
            <mvc:mapping path="/pinfo"/>
            <bean class="com.dhu.interceptor.AdminInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>